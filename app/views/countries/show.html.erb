<div>
  <div>
    <div>
      <h1><%= @country.title %></h1>
      <p><%= @country.description %></p>
    </div>
  </div>
  <div>
    <%= link_to 'View all countries', countries_path %>
    <%= link_to 'Edit', edit_country_path(@country)%>
    <%= link_to "Destroy", country_path(@country), data: {
        turbo_method: :delete,
        turbo_confirm: "Are you sure?"
    } %>
  </div>
</div>